/** "+ 색상 추가" 직후의 시드 라벨 (v1 handleAdd 보존) — 실제 팔레트 색은 DB(color_labels) 소관 */
export const NEW_COLOR_LABEL = '새 색상'

// eslint-disable-next-line no-restricted-syntax -- 추가 기본 hex는 토큰(UI 색)도 DB 값도 아닌 "새 색상" 시드 UI 동작 상수 (v1 handleAdd 보존, 명세 §진입·시트)
export const NEW_COLOR_HEX = '#888888'
